# Indent: 2 spaces, no tab
--indent=spaces=2
# Break brackets in "break" style
--style=break
# Align comments with the immediately followed line of code
--indent-col1-comments
# Add one blank line before and after 'if', 'for', 'while' blocks
--break-blocks
# Add one space before and after binary operators
--pad-oper
# Ensure no space around parentheses
--unpad-paren
# Attach a pointer or reference operator to the type, not the variable
--align-pointer=type
--align-reference=type
# Add brackets to unbracketed one line conditional statements
--add-brackets
# Remove the leading '*' prefix on multi-line comments
# and indent the comment text one indent
--remove-comment-prefix
# Break at logical positions to keep line length under 80
--max-code-length=80
--break-after-logical
# Excludes
--exclude=thirdparty
# Do not retain a backup of the original file.
--suffix=none
